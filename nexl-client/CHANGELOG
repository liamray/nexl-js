2.1.0 | 29-APR-2017
===================
    - fixed ^M character bug in nexl-client. nexl-client didn't work in Linux because of ^M characters in shell executable
    - added log management support to produce log file(s), control log levels and roll log files by size. nexl-client got the following new command line option :
        --log-level


2.0.1 | 01-APR-2017
===================
    - 2.x version is released
